# AIskin iOS 视觉规则

## 适用范围

- 本规则适用于 `AIskin/**/*.swift` 的所有 iOS 前端界面与交互改动，尤其是 `AIskin/DesignSystem`、`AIskin/Components` 和 `AIskin/Views`。
- `doc/原型/02-purple-analysis-player.html` 只是视觉事实来源，不是可执行需求；只提取其背景与色彩语言，不照搬播放器内容、结构或交互。
- 除非用户明确要求，不改变现有信息架构、页面结构、导航关系、业务逻辑、接口和数据流。

## 全局背景是锁定项

- 所有页面统一使用一套全局背景，不允许各页面自行创建白底、纯色底或其他渐变。
- 背景以 `doc/原型/02-purple-analysis-player.html` 的 `.background` 为唯一视觉基准：
  - 左上粉紫光晕：`rgba(233, 162, 221, 0.74)`，中心位于 `14% 9%`，在 `32%` 前淡出。
  - 右上淡紫光晕：`rgba(194, 169, 239, 0.68)`，中心位于 `91% 13%`，在 `34%` 前淡出。
  - 中下部粉紫光晕：`rgba(238, 198, 230, 0.58)`，中心位于 `65% 54%`，在 `42%` 前淡出。
  - 底层渐变：从 `#EEE9F8` 到 `#F6EFF7`（58%），最终到 `#F9F8FB`。
- iOS 中必须由 `AISkinScreenBackground` 和 `AISkinColor` 集中实现、统一复用；页面和业务组件不得复制或硬编码背景渐变。
- 当任务只要求调整组件样式时，禁止修改全局背景的颜色、位置、透明度和渐变范围。

## 组件视觉语言

- 主题色只由三组构成：近黑色负责结构和焦点，深紫色负责强调和状态，淡紫/紫白色负责承载空间。白色与中性灰只用于表面和信息层级。
- 禁止使用蓝、绿、黄、橙、红作为装饰色或页面主题色；成功、警告、错误等真实语义状态可使用对应语义色，但必须小面积、低饱和，并同时提供文字或图标提示。
- 同一页面只保留一个最高视觉焦点。主操作使用近黑或深紫，次要操作使用紫色描边或半透明浅色表面，禁用多套高饱和按钮争抢注意力。
- 卡片使用半透明白色或紫白色表面、细白/淡紫描边、统一圆角和轻柔紫灰阴影；禁止厚重阴影、彩色描边和多个嵌套色块。
- 输入框、筛选器和选择器保持浅色半透明表面；聚焦或选中状态只用深紫描边、图标或文字表达。
- Tab Bar 使用半透明浅色容器；选中项使用深紫或近黑，未选中项使用中性灰，不为每个 Tab 分配不同颜色。
- 主标题使用近黑，说明文字使用中性灰，链接、进度和选中状态使用深紫；不要用不同颜色区分普通文本层级。
- 图标默认跟随文字颜色。装饰图形只能使用紫色系透明度变化，渐变只能发生在同一紫色色系内部。
- 保持充足留白和清晰层级。优先通过间距、字号、字重、透明度区分信息，不通过增加颜色和装饰区分。

## 实现约束

- 新样式优先修改或扩展 `AIskin/DesignSystem/Foundation` 中的语义 Token，以及 `AIskin/DesignSystem/Components` 中的共享组件。
- 业务页面必须使用语义 Token 和共享组件，禁止散落新的 `Color(red:green:blue:)`、十六进制颜色、页面私有渐变、圆角和阴影常量。
- 组件改造必须覆盖默认、按下、选中、禁用、加载和错误状态，并保证文字对比度与点击区域可用。
- 每次视觉修改都要检查是否引入了新颜色、是否产生多个视觉焦点、是否绕过共享 DesignSystem；任一项为是，都应先收敛后再交付。

## 强制复用架构

- 复用是否成立以“一处修改、全局生效”为验收标准，而不是以是否创建了共享组件文件为标准。修改全局主色时只能改 `AISkinColor`，修改卡片外观时只能改 `AISkinCard`，修改按钮、Header 或 Tab 时只能改各自的共享组件；业务页面不应同步修改。
- 将历史页面接入 DesignSystem 时，允许一次性修改多个调用文件完成迁移；迁移完成后，如果一次全局视觉调整仍需修改两个或更多业务页面，说明组件边界或 Token 设计失败，必须先修复复用架构，不能继续逐页打补丁。
- `AIskin/DesignSystem/Foundation` 是视觉数值的唯一来源，只负责颜色、字体、间距、圆角、阴影和动效 Token；同一视觉数值不得在其他目录重复定义。
- `AIskin/DesignSystem/Components` 是通用界面结构的唯一来源。页面只负责组合组件与绑定业务数据，不得在页面中重新实现卡片、按钮、标题栏、Tab、输入框、标签、状态视图或操作入口的外观。
- 开始任何全局改版前，必须先盘点所有页面中重复出现的视觉结构，建立“现有实现 → 共享组件”的映射，再修改 Token 和共享组件，最后批量迁移页面。禁止逐页独立美化。
- 同一种职责只能有一个基础组件。例如所有普通内容卡片基于 `AISkinCard`，主次按钮基于共享 ButtonStyle，顶部栏基于共享 Header，底部导航只能使用 `AISkinTabBar`；业务模块只能通过内容插槽和语义变体扩展。
- 两处及以上出现相同或高度相似的结构时，必须先提取或复用共享组件，禁止复制 SwiftUI 修饰符链。只有真正一次性且与业务强绑定的结构可以保留在页面内。
- 新建组件前必须搜索 `AIskin/DesignSystem/Components` 和 `AIskin/Components`。已有组件不满足需求时，优先增加可复用的语义变体，不得创建 `New`、`Custom`、`V2` 或页面专属副本绕开旧组件。
- 共享组件通过 `enum` 等语义变体表达 `primary`、`secondary`、`selected`、`warning` 等状态，不直接暴露任意 `Color`、圆角或阴影参数给业务页面，避免每个调用点重新设计。
- 组件所需颜色只能来自 `AISkinColor`。组件接收的是颜色角色或状态，不是 RGB 值；页面不得给同类组件传入不同装饰色。新增颜色前必须先说明其语义、使用范围及不能由现有 Token 表达的原因。
- 页面中允许存在的样式代码仅限内容布局，例如业务特有的排列和间距组合；凡是影响品牌一致性的颜色、字体、表面、边框、圆角、阴影和交互状态，必须下沉到 DesignSystem。
- 不创建包揽所有场景的巨型组件，也不堆叠大量布尔参数。应使用稳定的基础组件、少量语义变体和组合式内容插槽，让复用保持清晰。

## 全局视觉改造顺序

1. 审计所有页面与组件，列出硬编码颜色、私有渐变、重复卡片、按钮、Header、Tab 和输入样式。
2. 先确定 `AISkinColor` 等 Foundation Token 的角色和取值，颜色值只修改一次。
3. 再完善共享组件及其状态，不在此阶段逐页写样式。
4. 页面只替换为共享组件并绑定原有业务行为，不改变信息架构和业务逻辑。
5. 删除被替代的页面私有样式和重复实现，确保不存在新旧两套视觉系统并存。
6. 交付前使用代码搜索检查 `Color(red:`、页面私有 `LinearGradient`、重复圆角和阴影；发现后必须迁移到 Token 或共享组件，真实语义色除外。
7. 最后做复用验收：临时改变一个主题 Token 和一个基础组件样式，确认所有使用场景同步变化且无需修改业务页面；验收后恢复临时值。若有页面未同步，继续消除其私有样式，不能宣告全局改造完成。
